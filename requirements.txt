# Core runtime dependencies. Mirrors the [project.dependencies] block
# in pyproject.toml. For optional extras (segmentation, scvi, celltypist,
# spatialdata, dev) install via, e.g., `pip install -e ".[segmentation]"`.

scanpy>=1.10.0
anndata>=0.10.0
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
h5py>=3.9
tables>=3.9
leidenalg>=0.10
python-igraph>=0.11
harmonypy>=0.0.10
squidpy>=1.5.0
pyyaml>=6.0
tqdm>=4.66
click>=8.1
Pillow>=10.0
